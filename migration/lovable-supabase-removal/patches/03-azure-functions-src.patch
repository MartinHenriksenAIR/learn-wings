# PATCH 03 — Azure Functions source implementation
# DRAFT ONLY — PLACEHOLDER. NOT IMPLEMENTABLE until auth provider is decided.
#
# This patch covers the creation of the `functions/` directory containing all
# 10 replacement Azure Functions (Node.js 22, HTTP-triggered).
#
# WHY THIS IS A PLACEHOLDER:
# Every function must validate JWTs. The JWT validation logic (public key endpoint,
# issuer claim, audience claim, user ID extraction) is entirely determined by which
# auth provider replaces Supabase Auth. This decision is OPEN QUESTION Q1 in
# migration/lovable-supabase-removal/10-open-questions.md.
#
# WHAT THIS PATCH WILL CONTAIN (once auth is decided):
#
#   functions/
#   ├── host.json                      — Azure Functions host config (extensionBundle, version)
#   ├── package.json                   — Node.js deps: @azure/functions, pg, pdf-lib, resend, crypto
#   ├── shared/
#   │   ├── auth.ts                    — JWT validation (provider-specific implementation)
#   │   ├── db.ts                      — PostgreSQL client (pg Pool, env: PGHOST/PGUSER/PGPASSWORD/PGDATABASE)
#   │   ├── cors.ts                    — CORS helper (allowlist: [ai-uddannelse.dk] only)
#   │   └── sas.ts                     — Azure Blob SAS generation (Node.js crypto, sv=2022-11-02)
#   ├── grade-quiz/
#   │   └── index.ts                   — Decision B: implement in func-ai-education-migration
#   ├── generate-certificate/
#   │   └── index.ts                   — Decision B: implement in func-ai-education-migration
#   ├── delete-user/
#   │   └── index.ts                   — Decision B: implement in func-ai-education-migration
#   ├── send-invitation-email/
#   │   └── index.ts                   — Decision B: implement in func-ai-education-migration
#   ├── azure-upload-url/
#   │   └── index.ts                   — Decision C: natural Azure blob owner
#   ├── azure-view-url/
#   │   └── index.ts                   — Decision C: natural Azure blob owner
#   ├── azure-delete-blob/
#   │   └── index.ts                   — Decision C: natural Azure blob owner
#   ├── generate-compliance-report/
#   │   └── index.ts                   — Decision B: implement in func-ai-education-migration
#   ├── azure-document-upload-url/
#   │   └── index.ts                   — Decision C: natural Azure blob owner
#   └── test-smtp-connection/
#       └── index.ts                   — Decision B: implement in func-ai-education-migration
#                                         CRITICAL: must add auth gate (currently has NO auth)
#
# FULL CODE SPECIFICATIONS:
# See migration/lovable-supabase-removal/06-proposed-code-changes.md for:
# - Per-function input/output contracts
# - SAS generation port (Deno HMAC → Node.js crypto)
# - CORS restriction to ai-uddannelse.dk only
# - PostgreSQL query replacements for each Supabase RPC
# - PDF generation (port from Deno TextEncoder → Node.js Buffer)
# - Auth gate pattern for all functions including test-smtp-connection
#
# NEXT STEP:
# 1. Resolve Q1 (auth provider) in 10-open-questions.md
# 2. Implement shared/auth.ts with provider-specific JWT validation
# 3. Implement all 10 functions using shared/* helpers
# 4. Apply patches/05-ci-cd-workflow.patch to update GitHub Actions
# 5. Apply patches/04-frontend-callsites.patch to update frontend
